{% extends 'base.html.twig' %}

{% block title %}Categories{% endblock %}

{% block body %}

  <h1 class="text-3xl mb-4">Categories</h1>

  {{ component('breadcrumbs', { 'items': [
    { 'name': 'Transactions', 'path': 'app_transaction.index' },
    { 'name': 'Categories' }
  ] }) }}

  <a href="{{ path('app_category.create') }}" class="btn gap-2">
    <span class="material-symbols-outlined">add</span>
    Create category
  </a>

  {{ component('categories-table', {
    'categories': categories
  }) }}

{% endblock %}
