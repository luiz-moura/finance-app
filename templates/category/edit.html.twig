{% extends 'base.html.twig' %}

{% block title %}Category edit{% endblock %}

{% block body %}

  <h1 class="text-3xl mb-4">Category edit</h1>

  {{ component('breadcrumbs', { 'items': [
    { 'name': 'Transactions', 'path': 'app_transaction.index' },
    { 'name': 'Categories', 'path': 'app_category.index' },
    { 'name': 'Edit category' }
  ] }) }}

  <form action="{{ path('app_category.update', { id: category.id }) }}" method="post">
    <input type="hidden" name="_method" value="PUT">
    {% include 'category/form.html.twig' %}
  </form>

{% endblock %}
